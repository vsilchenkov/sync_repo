Функция БулевоИзСтроки(Значение) Экспорт
	
	Если Значение <> Неопределено Тогда
		ЗначениеПреобразованное = Булево(Значение);		
	Иначе
		ЗначениеПреобразованное = Ложь;		
	КонецЕсли;
	
	Возврат ЗначениеПреобразованное;
	
КонецФункции